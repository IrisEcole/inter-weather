//Can't test until figured out firebase auth testing

/* import DeleteAccountForm from '@/app/components/DeleteAccountForm';
import { render } from '@testing-library/react-native';

jest.mock("@expo/vector-icons", () => ({
  Ionicons: "",
  Icon: "",
  MaterialCommunityIcons: ""
}));

describe('<DeleteAccountForm />', () => {
  test('Text renders correctly on DeleteAccountForm', async () => {
    const { getByText } = render(<DeleteAccountForm />);
    getByText('Email');
    getByText('Password');
    getByText('Delete');
    getByText('Close');
  });
});
 */